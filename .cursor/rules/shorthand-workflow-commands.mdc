# Cursor Shorthand Workflow Commands

When the user writes one of the shorthand commands below, interpret it exactly as follows:

- `crs` = check, report, and suggest. Do not rush to implementation.
- `rt pl` = return an implementation plan, using planning mode.
- `iml` = implement the last suggested plan.
